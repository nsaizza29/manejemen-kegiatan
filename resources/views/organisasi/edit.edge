<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Organisasi - Manajemen Kegiatan</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    @include('components/navigation')

    <!-- Main Content -->
    <main class="container my-4">
        @include('components/flash-messages')

        <!-- Header -->
        <div class="row mb-4">
            <div class="col-12">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/dashboard">Dashboard</a></li>
                        <li class="breadcrumb-item"><a href="/organisasi">Organisasi</a></li>
                        <li class="breadcrumb-item active">Edit Organisasi</li>
                    </ol>
                </nav>
                <h1 class="h3 mb-0">
                    <i class="fas fa-edit me-2"></i>Edit Organisasi
                </h1>
                <p class="text-muted">Ubah informasi organisasi yang dipilih</p>
            </div>
        </div>

        <!-- Form -->
        <div class="row">
            <div class="col-lg-8">
                <div class="card">
                    <div class="card-body">
                        <form action="/organisasi/{{ organisasi.id }}/update" method="POST">
                            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
                            
                            <div class="mb-3">
                                <label for="nama_organisasi" class="form-label">Nama Organisasi <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" id="nama_organisasi" name="nama_organisasi" value="{{ organisasi.nama_organisasi }}" required>
                            </div>

                            <div class="mb-3">
                                <label for="jenis" class="form-label">Jenis Organisasi <span class="text-danger">*</span></label>
                                <select class="form-select" id="jenis" name="jenis" required>
                                    <option value="">Pilih Jenis Organisasi</option>
                                    <option value="Himpunan Mahasiswa" {{ organisasi.jenis == 'Himpunan Mahasiswa' ? 'selected' : '' }}>Himpunan Mahasiswa</option>
                                    <option value="Badan Eksekutif Mahasiswa" {{ organisasi.jenis == 'Badan Eksekutif Mahasiswa' ? 'selected' : '' }}>Badan Eksekutif Mahasiswa</option>
                                    <option value="Unit Kegiatan Mahasiswa" {{ organisasi.jenis == 'Unit Kegiatan Mahasiswa' ? 'selected' : '' }}>Unit Kegiatan Mahasiswa</option>
                                    <option value="Lainnya" {{ organisasi.jenis == 'Lainnya' ? 'selected' : '' }}>Lainnya</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label for="deskripsi" class="form-label">Deskripsi</label>
                                <textarea class="form-control" id="deskripsi" name="deskripsi" rows="4" placeholder="Deskripsi organisasi...">{{ organisasi.deskripsi || '' }}</textarea>
                            </div>

                            <div class="mb-3">
                                <label for="alamat" class="form-label">Alamat</label>
                                <textarea class="form-control" id="alamat" name="alamat" rows="3" placeholder="Alamat organisasi...">{{ organisasi.alamat || '' }}</textarea>
                            </div>

                            <div class="mb-3">
                                <label for="no_telepon" class="form-label">No. Telepon</label>
                                <input type="text" class="form-control" id="no_telepon" name="no_telepon" value="{{ organisasi.no_telepon || '' }}" placeholder="Nomor telepon organisasi">
                            </div>

                            <div class="d-flex gap-2">
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-save me-1"></i>Update Organisasi
                                </button>
                                <a href="/organisasi" class="btn btn-secondary">
                                    <i class="fas fa-times me-1"></i>Batal
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="fas fa-info-circle me-2"></i>Informasi Organisasi
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="text-muted small">
                            <strong>ID:</strong> {{ organisasi.id }}
                        </p>
                        <p class="text-muted small">
                            <strong>Dibuat:</strong> {{ organisasi.createdAt.toFormat('dd/MM/yyyy HH:mm') }}
                        </p>
                        <p class="text-muted small">
                            <strong>Diperbarui:</strong> {{ organisasi.updatedAt.toFormat('dd/MM/yyyy HH:mm') }}
                        </p>
                        <hr>
                        <p class="text-muted small">
                            <strong>Nama Organisasi:</strong> Masukkan nama organisasi yang jelas dan lengkap.
                        </p>
                        <p class="text-muted small">
                            <strong>Jenis Organisasi:</strong> Pilih jenis organisasi yang sesuai dengan kategori yang tersedia.
                        </p>
                        <p class="text-muted small">
                            <strong>Deskripsi:</strong> Jelaskan visi, misi, dan tujuan organisasi.
                        </p>
                        <p class="text-muted small">
                            <strong>Alamat:</strong> Masukkan alamat kantor atau sekretariat organisasi.
                        </p>
                        <p class="text-muted small">
                            <strong>No. Telepon:</strong> Masukkan nomor kontak organisasi untuk keperluan komunikasi.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 